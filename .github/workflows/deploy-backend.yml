name: Deploy backend

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "supabase/functions/generate-document/**"
      - "supabase/functions/generate-protocol-document/**"
      - "supabase/functions/_shared/**"
      - "supabase/migrations/20260921000300_add_generated_document_issuer.sql"
      - ".github/workflows/deploy-backend.yml"

concurrency:
  group: production-backend
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ secrets.VPS_PORT }}" "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload backend release
        env:
          REMOTE_RELEASE: "/tmp/hse-backend-${{ github.sha }}"
        run: |
          ssh -i ~/.ssh/deploy_key -p "${{ secrets.VPS_PORT }}" "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "mkdir -p '$REMOTE_RELEASE/functions'"
          scp -i ~/.ssh/deploy_key -P "${{ secrets.VPS_PORT }}" -r \
            supabase/functions/generate-document \
            supabase/functions/generate-protocol-document \
            supabase/functions/_shared \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:$REMOTE_RELEASE/functions/"
          scp -i ~/.ssh/deploy_key -P "${{ secrets.VPS_PORT }}" \
            supabase/migrations/20260921000300_add_generated_document_issuer.sql \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:$REMOTE_RELEASE/migration.sql"

      - name: Apply migration and restart Edge Runtime
        env:
          REMOTE_RELEASE: "/tmp/hse-backend-${{ github.sha }}"
          DOCUMENT_SCRIPT_URL: "${{ secrets.GOOGLE_APPS_SCRIPT_URL }}"
          PROTOCOL_SCRIPT_URL: "${{ secrets.GOOGLE_APPS_SCRIPT_PROTOCOL_URL }}"
        run: |
          test -n "$DOCUMENT_SCRIPT_URL"
          test -n "$PROTOCOL_SCRIPT_URL"
          ssh -i ~/.ssh/deploy_key -p "${{ secrets.VPS_PORT }}" "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" \
            bash -s -- "$REMOTE_RELEASE" "$DOCUMENT_SCRIPT_URL" "$PROTOCOL_SCRIPT_URL" <<'REMOTE'
          set -euo pipefail

          release_path="$1"
          document_script_url="$2"
          protocol_script_url="$3"

          edge_id="$(sudo -n docker ps --filter label=com.docker.compose.service=functions -q | head -n 1)"
          test -n "$edge_id"
          functions_source="$(sudo -n docker inspect "$edge_id" --format '{{range .Mounts}}{{if eq .Destination "/home/deno/functions"}}{{.Source}}{{end}}{{end}}')"
          test -n "$functions_source"

          for function_name in generate-document generate-protocol-document _shared; do
            sudo -n mkdir -p "$functions_source/$function_name"
            sudo -n cp -a "$release_path/functions/$function_name/." "$functions_source/$function_name/"
          done

          compose_dir="$(sudo -n docker inspect "$edge_id" --format '{{index .Config.Labels "com.docker.compose.project.working_dir"}}')"
          compose_service="$(sudo -n docker inspect "$edge_id" --format '{{index .Config.Labels "com.docker.compose.service"}}')"
          env_file="$compose_dir/.env"
          sudo -n test -f "$env_file"

          update_env() {
            key="$1"
            value="$2"
            if sudo -n grep -q "^${key}=" "$env_file"; then
              sudo -n sed -i "s|^${key}=.*$|${key}=${value}|" "$env_file"
            else
              printf '%s=%s\n' "$key" "$value" | sudo -n tee -a "$env_file" >/dev/null
            fi
          }

          update_env GOOGLE_APPS_SCRIPT_URL "$document_script_url"
          update_env GOOGLE_APPS_SCRIPT_PROTOCOL_URL "$protocol_script_url"

          db_id="$(sudo -n docker ps --filter label=com.docker.compose.service=db -q | head -n 1)"
          test -n "$db_id"
          sudo -n docker exec -i "$db_id" psql -v ON_ERROR_STOP=1 -U postgres -d postgres < "$release_path/migration.sql" >/dev/null

          sudo -n docker compose --project-directory "$compose_dir" up -d --no-deps "$compose_service" >/dev/null
          rm -rf "$release_path"
          REMOTE
